/*let arr1 = ['Earth', 'Moon', 'Mars'];
function first(array) {
  return array[0];
}

console.log(first(arr1)); // 'Earth'

let arr2 = ['Earth', 'Moon', 'Mars'];
function last(array) {
  return array[array.length-1];
}

console.log(last(arr1)); // 'Mars'*/

/*let energy = ['fossil', 'solar', 'wind', 'tidal', 'fusion'];
let removeElement = energy.shift();
let addElement = energy.push('geothermal');
console.log(energy);*/

/*let alphabet = 'abcdefghijklmnopqrstuvwxyz';
console.log(alphabet.split(''));*/

/*let scores = [96, 47, 113, 89, 100, 102];
//let findNum = scores.filter(element => element>=100);
//console.log(findNum.length);
let count = 0;
for (i = 0; i < scores.length; i += 1) {
  if (scores[i] >= 100) {
    count += 1;
  }
}

console.log(count);*/

/*let vocabulary = [
  ['happy', 'cheerful', 'merry', 'glad'],
  ['tired', 'sleepy', 'fatigued', 'drained'],
  ['excited', 'eager', 'enthused', 'animated']
];

for (let i = 0; i < vocabulary.length; i += 1) {
  for (let j = 0; j < vocabulary[i].length; j += 1) {
      console.log(vocabulary[i][j]);
  }
}

vocabulary.forEach(subArray => {
  subArray.forEach(word => console.log(word));
})*/

/*let destinations = ['Prague', 'London', 'Sydney', 'Belfast', 'Rome',
  'Aruba', 'Paris', 'Bora Bora', 'Barcelona', 'Rio de Janeiro',
  'Marrakesh', 'New York City'];

  function contain(city) {
   let arr1 = destinations.filter(element => element === city);
    if (arr1[0] === city) {
      return true;
    }else if (arr1[0] !== city) {
      return false;
    }
  }
console.log(contain('New York City'));*/



/*let passcode = ['11', 'jZ5', 'hQ3f*', '8!7g3', 'p3Fs'];
let passcodeGenerator = passcode.join('-');
console.log(passcodeGenerator);*/

/*let groceryList = ['paprika', 'tofu', 'garlic', 'quinoa', 'carrots', 'broccoli', 'hummus'];
for (let i = 0; groceryList.length > 0;) {
  console.log(groceryList.shift());
}

console.log(groceryList);*/

  
  

